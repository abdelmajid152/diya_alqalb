import 'package:flutter/material.dart';

/// App color constants for light and dark themes
class AppColors {
  // Primary colors
  static const Color primaryLight = Color(0xFF17683B);
  static const Color primaryDark = Color(0xFF259956);

  // Secondary colors
  static const Color secondaryLight = Color(0xFF2E8B57);
  static const Color secondaryDark = Color(0xFF3CB371);

  // Background colors
  static const Color backgroundLight = Color(0xFFFEFCF4);
  static const Color backgroundDark = Color(0xFF141519);

  // Surface colors
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1E1E24);

  // Text colors
  static const Color textPrimaryLight = Color(0xFF1A1A2E);
  static const Color textPrimaryDark = Color(0xFFF5F5F5);

  static const Color textSecondaryLight = Color(0xFF6B7280);
  static const Color textSecondaryDark = Color(0xFF9CA3AF);

  // Accent colors
  static const Color accentLight = Color(0xFF17683B);
  static const Color accentDark = Color(0xFF259956);

  // Gradient colors
  static const List<Color> gradientLight = [
    Color(0xFF17683B),
    Color(0xFF2E8B57),
  ];

  static const List<Color> gradientDark = [
    Color(0xFF259956),
    Color(0xFF3CB371),
  ];
}
